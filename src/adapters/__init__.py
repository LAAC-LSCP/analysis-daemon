"""
The adapters module contains all code that connects our domain and service layer,
i.e., that interfaces the domain and service layer. This includes code at the lowest
relevant level (the database), the interface to which is handled by SQLAlchemy
"""
