"""
The configuration module handles all configuration.
The configuration is handled by pydantic. The configuration is read from a toml file,
and includes such things as which datasets are available on the system, with which
parameters to run each model, what job scheduler we use, and so on
"""
